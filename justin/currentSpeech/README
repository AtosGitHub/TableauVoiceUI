// from folder justin/currentSpeech

To Run: (pick one)

1) open getData.html

2) open 'select_workbook.html', select 'Workbook01' from drop-down, you will be directed to the Tableau page (getData.html).



// usage for Speech team

button: "printSheetList" prints SheetList array, but only previously visited tabs will have 'fields' array populated; it is populated automatically when the tab is visited

button: "printActive" prints information for the active sheet and demonstrates what you receive if you call 'SheetList[activeIndex]', which can also be tested at the console by entering 'SheetList[activeIndex]'